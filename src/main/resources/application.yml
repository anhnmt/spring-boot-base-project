server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:camunda
    username: sa
    password:
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true